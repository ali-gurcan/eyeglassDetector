version: '3.8'

services:
  eyeglass-detector:
    build: .
    container_name: eyeglass-detector
    volumes:
      - ./images:/app/images
      - ./output:/app/output
      - ./debug:/app/debug
    command: python src/main.py

